import { Plus } from "lucide-react";

const PlusButton = () => {
  return (
    <Plus size={20} className="fill-Gray-700 stroke-Gray-700 hover:bg-Line-Container-Line rounded-[0.25rem] cursor-pointer" />
  );
};

export default PlusButton;
